# AG-UI endpoint (primary — used by LangGraphHttpAgent)
REMOTE_ACTION_URL=http://localhost:8000/agent

# Backend API base URL for REST endpoints (auth, fortune, diary)
NEXT_PUBLIC_API_BASE=http://localhost:8000
